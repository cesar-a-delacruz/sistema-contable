1. `orphanRemoval = true` si se elimina el elemento de la lista, se borrara de la base de datos, por lo general no se quiere esto.
2. `cascade = CascadeType.PERSIST` se persiste elementos nuevos, por lo general no se quieres est, mejor quitar el cascade