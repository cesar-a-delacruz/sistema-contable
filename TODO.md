1. hacer que `setEntityDataFromForm` de [SimpleView](./src/main/java/com/nutrehogar/sistemacontable/ui/SimpleView.java) se le pueda pasar una `Session`.
2. Hacer DTO`s para los ComboBox por ejemplos: el de `AccountView` para subTipos.
3. hacer que cuando se va a persistir algo se tenga que buscar primero en la base de datos.